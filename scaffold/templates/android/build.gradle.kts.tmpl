// build.gradle.kts (root) — top-level plugin declarations for ${PROJECT_NAME}.
// Plugins are declared `apply false` here and applied per-module in
// app/build.gradle.kts. AGP/Kotlin/ktlint versions chosen to be stable on
// Android Studio Koala/Jellyfish + Gradle 8.7. See `gradle.properties` for
// shared Gradle JVM args.

plugins {
    id("com.android.application") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.0" apply false
    id("org.jetbrains.kotlin.plugin.compose") version "2.0.0" apply false
    id("org.jlleitschuh.gradle.ktlint") version "12.1.1" apply false
}
