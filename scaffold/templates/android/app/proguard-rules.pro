# Add ProGuard / R8 rules here. Currently the release build does not minify
# (see app/build.gradle.kts), so this file is a placeholder. Enable
# isMinifyEnabled and tune rules before shipping a release build.
