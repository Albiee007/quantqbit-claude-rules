# ${PROJECT_NAME} — example environment for Expo.
# Copy to .env and fill in real values for local development.
# NEVER commit .env (already covered by .gitignore).
#
# Expo only exposes vars prefixed with EXPO_PUBLIC_ to JS. See
# https://docs.expo.dev/guides/environment-variables/ for details.

# --- API ---
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# --- Feature flags (optional) ---
EXPO_PUBLIC_FEATURE_DEBUG=false
