// useAuth.ts
// Placeholder auth hook — replace with real provider context (Auth0 Provider,
// Cognito hooks, etc.). Kept as a stub so the feature folder is buildable.

import type { AuthSession } from '../types/auth.types';

export function useAuth(): { session: AuthSession | null } {
  return { session: null };
}
