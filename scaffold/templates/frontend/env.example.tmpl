# ${PROJECT_NAME} — example environment.
# Copy to .env (or .env.local) and fill in real values for local development.
# Only VITE_-prefixed variables are exposed to client code by Vite.
# NEVER commit .env / .env.local (already covered by .gitignore).

# --- API ---
VITE_API_BASE_URL=http://localhost:3000
