# ${PROJECT_NAME} — System Architecture

> Stub stamped on ${TIMESTAMP}. Replace as the architecture takes shape.

## Overview

One paragraph: what the system does, who uses it, where it runs.

## Components

| Component | Responsibility | Runtime |
| --- | --- | --- |
| _example_ | _what it owns_ | _node / browser / android_ |

## Data flow

```
client ──▶ api ──▶ service ──▶ datastore
                       │
                       └──▶ external integration
```

## Decisions

- _Decision 1_ — chosen over _alternative_ because _reason_.
- _Decision 2_ — locked at the start of the project; revisit only if _trigger_.

## Open questions

- TODO: list open architectural questions.
