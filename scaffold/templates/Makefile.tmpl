.PHONY: help lint

help:
	@echo "Targets:"
	@echo "  lint   Run the full lint suite (shellcheck, yamllint, ansible-lint, terraform, compose)"

lint:
	@bash scripts/lint.sh   # run make from this directory (make -C <dir> lint)
