.PHONY: help lint

help:
	@echo "Targets:"
	@echo "  lint   Run the full lint suite (shellcheck, yamllint, ansible-lint, terraform, compose)"

lint:
	@bash "$(dir $(abspath $(lastword $(MAKEFILE_LIST))))scripts/lint.sh"
